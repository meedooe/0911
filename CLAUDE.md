# CLAUDE.md — D2R 극블리 소서 파밍 대시보드

> 이 문서는 **경량 모델(Flash-Lite 등)이 이 코드베이스를 유지보수할 때 반드시 먼저 읽어야 하는 규칙서**다.
> 여기 적힌 규칙을 어기면 구조가 무너진다. 아래 규칙을 그대로 따를 것.

---

## 0. 이 프로젝트가 뭔가

로컬에서 여는 **정적 HTML 페이지 두 개**로 이루어져 있다. 빌드 도구 없음. 번들러 없음.
프레임워크 없음. npm 설치 없음. 순수 HTML + CSS + Vanilla JS (ES2020) 뿐이다.

**2026-09-24부터 역할이 나뉘어 있다 — 아래 표를 먼저 읽어라.**

| 파일 | 역할 | 구조 |
|---|---|---|
| `guide.html` | **메인 공략서.** 캐릭터 진단, 실행 순서, 지역 티어, 장비 현상수배, 치트시트/계산기, 도박 안내, 스탯·기술 초기화 가이드 등 **콘텐츠 전부**가 여기 있다. 아이템명·수치는 인게임/Inven DB 대조로 계속 갱신된다. | 단일 파일에 `<style>`+`<script>`가 전부 인라인. 아래 2~5장의 모듈 규칙(01~05 분리, JSDoc 등)은 **이 파일에는 적용되지 않는다** — 섹션 단위(`<section id="...">`)로 구성돼 있으니 해당 섹션 안에서 고쳐라. |
| `index.html` + `js/01~05-*.js` | **스피드런 랩타임 타이머 하나만** 담당하는 보조 도구. 맵 티어/현상수배/치트시트/막힘 가이드는 전부 `guide.html`로 이전됐다. | 아래 1~5장 규칙 그대로 적용 (모듈 분리, 순수 함수, 테스트). |

**콘텐츠(아이템 스탯, 파밍 우선순위, 빌드 조언 등) 관련 요청은 `guide.html`을 고쳐라.**
`index.html` 쪽은 타이머 기능 자체를 바꿔달라는 요청에만 손댄다. 둘을 다시 하나로 합치거나
콘텐츠를 양쪽에 중복해서 넣지 마라 — 그러면 곧바로 값이 어긋난다(레벨/저항/난이도 같은 것들).

### guide.html을 고칠 때 추가로 지킬 것
- **아이템명은 현재 디아블로2: 레저렉션 한글판 표기와 반드시 일치해야 한다.** 음역(영어 발음을 한글로
  옮긴 커뮤니티 표기, 예: "인피니티"·"에니그마")이 아니라 인게임에 실제로 뜨는 이름("무한"·"수수께끼")을 써라.
  확신이 없으면 [디아블로2 레저렉션 인벤 아이템 DB](https://diablo2.inven.co.kr/dataninfo/item/)나
  웹검색으로 대조하고, 그래도 불확실하면 `<span class="badge b-approx">근사치</span>` 배지를 붙여라.
- 클래스 약어(FCR, FHR, MF 등)를 새로 쓸 때는 처음 나오는 자리에
  `<abbr class="gloss" title="...">MF</abbr>`처럼 풀어써서 툴팁을 달아라 — TMI(정보 과다)는
  괜찮지만 설명 없는 약어로 막히게 하지 마라.
- 캐릭터 진단(히어로 영역, `#diagnosis`, `#now` 등)은 **그 시점의 스냅샷**이다. 상황이 바뀌면(문제
  해결, 레벨업, 장비 교체) 관련 섹션에 `해결됨`/`지난 기록` 배지를 달고 최신 섹션(`#solutions` 등)으로
  링크를 걸어라 — 옛 내용을 그냥 방치하면 서로 다른 섹션이 모순된 조언을 하게 된다.

---

## 1. 파일 구조와 각 파일의 책임 — 절대 섞지 말 것

```
0911/
├── guide.html           # ★메인 공략서★ 단일 파일, 인라인 style+script. 콘텐츠는 다 여기.
├── index.html            # 스피드런 타이머 마크업 + <script> 로드 순서. 로직 금지.
├── css/style.css         # index.html 스타일 전부. JS에서 style 직접 조작 금지, class 토글만.
├── js/01-state.js        # [상태] 런타임 상태 객체 STATE(타이머) + 저장소 어댑터.
├── js/02-data.js         # [데이터] META_DATA.terrorZones — 타이머 측정 대상 지역 목록.
├── js/03-logic.js        # [로직] 순수 함수만(시간 포맷, 세션 통계, 페이스 평가). DOM 접근 금지.
├── js/04-render.js       # [렌더] DOM 생성/갱신만. 계산 금지. 03-logic 결과를 받아 그리기만.
├── js/05-main.js         # [부트] 초기화 + 이벤트 바인딩 + 타이머 루프.
└── test/logic.test.mjs   # 03-logic 순수 함수 단위 테스트 (node test/logic.test.mjs)
```

**파일 번호는 `index.html`의 로드 순서다. 새 파일을 끼워 넣지 말고 기존 파일 안에서 작업해라.**
이 번호 규칙과 아래 2~5장은 `js/01~05-*.js`에만 적용된다. `guide.html`은 완전히 다른 구조(섹션별
인라인 코드)이니 0장 끝의 "guide.html을 고칠 때" 항목을 따라라.

---

## 2. 경량 모델이 지켜야 할 5가지 규칙

### 규칙 1 — 앵커 주석 안쪽에만 코드를 넣어라
코드 곳곳에 아래 형태의 앵커가 있다.

```js
// [TODO: LITE_MODEL_INSERT_FEATURE_HERE — <설명>]
```

새 기능/새 데이터는 **관련된 앵커 바로 아래**에 추가한다.
앵커 주석 자체는 **절대 삭제하지 마라**. 추가 후에도 앵커는 남겨둔다.

### 규칙 2 — 데이터 추가는 `js/02-data.js`에서만
아이템, 맵, 룬 추가는 **코드를 짜는 게 아니라 배열에 객체 1개를 복사해 넣는 일**이다.
각 배열 바로 위에 `@typedef` JSDoc으로 스키마가 정의돼 있다.
**기존 항목 하나를 복사 → 값만 바꾸기.** 키를 새로 만들거나 빼지 마라.

### 규칙 3 — 로직과 렌더를 섞지 마라
- 숫자 계산·시간 계산·필터링 → `03-logic.js`에 **순수 함수**로 추가 (입력→출력, 부작용 없음)
- 화면에 그리는 일 → `04-render.js`
- `03-logic.js` 안에서 `document`, `window`, `STATE`를 직접 읽으면 **잘못된 코드다.** 인자로 받아라.

### 규칙 4 — JSDoc 필수
모든 함수에 아래 형식을 붙인다. 생략 금지.

```js
/**
 * 한 줄 설명.
 * @param {number} a - 설명
 * @returns {number} 설명
 */
```

### 규칙 5 — 수치를 지어내지 마라
게임 수치(데미지, 확률, 드랍률)는 검증된 것만 넣는다.
불확실하면 데이터 객체의 `verified: false` 와 `note` 필드에 근거/불확실성을 남기고,
UI에는 "근사치" 표기가 자동으로 붙는다. **없는 수치를 그럴듯하게 채우지 마라.**

---

## 3. 자주 하게 될 작업 레시피

### A. 공포의 영역(맵) 추가 — 타이머 측정 대상 지역
`js/02-data.js` → `META_DATA.terrorZones` 배열 → 기존 항목 복사 후 값 수정.
`tier`는 `"S" | "A" | "B" | "D"` 중 하나만(참고용 표시일 뿐, 실제 티어 판단 기준은 `guide.html`).
지역이 새로 필요하면 **`guide.html`의 `#zones` 표에도 같이 추가**해야 두 문서가 어긋나지 않는다.

### B. 현상수배(졸업 장비)·치트시트·도박 안내 관련 요청
전부 `guide.html` 안의 해당 `<section>`(`#gear`, `#cheatsheet`, `#calc`, `#gambling`)을 고쳐라.
`index.html`/`js/*`에는 더 이상 이 기능들이 없다 — 새로 만들지 마라.

### C. 새 섹션(콘텐츠) 추가
`guide.html`에 새 `<section id="...">`를 만들고 상단 `<nav>`에 앵커 링크를 추가해라.
체크리스트가 필요하면 기존 `.cklist`/`data-ck` 패턴(경로 선택 섹션 참고)을 그대로 따라 쓰면
자동 저장(localStorage)이 같이 딸려온다.

### D. 타이머 도구 자체에 기능 추가 (index.html)
1. `js/02-data.js`에 데이터가 필요하면 추가
2. `03-logic.js` 하단 앵커에 순수 함수 추가 → `test/logic.test.mjs`에 테스트 1개 추가
3. `04-render.js`에 렌더 함수 추가 → `05-main.js`의 `renderAll()`/`init()`에 호출 추가
4. `index.html`의 `<!-- [TODO: LITE_MODEL_INSERT_FEATURE_HERE — 새 탭 패널] -->`에 마크업 추가

---

## 4. 하면 안 되는 것

- ❌ 외부 CDN / 라이브러리 추가 (오프라인 로컬 실행이 전제)
- ❌ 빌드 스텝 도입 (webpack/vite/ts 등)
- ❌ `innerHTML` 에 사용자 입력을 그대로 넣기 → `esc()` 헬퍼를 써라
- ❌ `03-logic.js` 에서 DOM 접근
- ❌ 앵커 주석 삭제
- ❌ 검증 안 된 게임 수치를 확정값처럼 넣기

---

## 5. 검증 방법

```bash
node test/logic.test.mjs     # 순수 로직 테스트. 전부 PASS 떠야 한다.
```
그 다음 브라우저에서 **고친 파일 쪽**(`index.html` 또는 `guide.html`)을 열고 콘솔 에러 0개 확인.
`guide.html`은 테스트가 없으니 눈으로 직접 확인하는 게 유일한 검증 수단이다 — 체크리스트 클릭,
계산기 입력, 테마 전환까지 한 번씩 눌러봐라.

---

## 6. 데이터 출처 (2026-09-11 기준 리서치)

- Maxroll — Blizzard Sorceress 빌드 / Breakpoints / Rune Value
- DiabloBytes — Terror Zone 가이드
- Diablo Wiki (Fandom) — Blizzard 스킬 데미지/시너지

**거래 "가격"은 의도적으로 넣지 않는다.** 시세는 시즌마다 변해서 하드코딩하면 금방 거짓말이 된다.
대신 `tradeTier`(T0~T3, 상대적 수요 등급)만 유지한다. 등급 갱신 시 `META_DATA.lastResearched` 날짜도 같이 고쳐라.
